function createLottoArray(lotto, outOf) {
  // Kumar

  // write a function, which accepts to parameters
  // lotto - number of lotto numbers
  // outOf - the every possible number
  // and returns an arrays of 6 out of 49 should return random six numbers (not repeating)
  // starting from 1 to inclusive 49
}

// Azat
// write test which gives different numbers of lotto and tests the returning array length
// and the numbers in the arrays
